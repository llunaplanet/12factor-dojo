version: '3'
services:

  sut-a:
    image: sut
    stop_signal: SIGKILL
    environment:
      - PORT=80
      - MOT=Aupaaaa!
      - REDIS_URI=redis://passwordRedis@redis/0
  
  redis:
    image: "redis:5.0-alpine"
    command: --requirepass passwordRedis
