version: '3'
services:

  sut-a:
    image: sut
    environment:
      - PORT=80
      - MOT=Aupaaaa!
      - ENVIRONMENT=staging
  
  sut-b:
    image: sut
    environment:
      - PORT=80
      - MOT=Aupaaaa!
      - ENVIRONMENT=qa
